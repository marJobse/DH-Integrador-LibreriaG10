<!DOCTYPE html>
<html lang="es">

<%- include('partials/head.ejs') %>

    <body>
        <%- include('partials/header.ejs') %>

            <main>
                <section class="oferta">
                    <a href="/productDetail"><img src="images/oferta.jpg" alt="oferta">
                </section></a>
                <h2>Más Vendidos</h2>
                <section class="product-section flex">
                    <% for(let i=0; i < products.length; i++) { %>
                    <div class="product-item">
                        <a href="/product/detail/<%= products[i].id %>>"><img class="product-item-img" src="/images/products/<%= products[i].imagen %>" alt=""></a>
                        
                        <h4><a href="/product/detail/<%= products[i].id %>>"><%= products[i].nombre %></a></h4>
                        <p>$<%= products[i].precio %></p>
                        <a id="boton-comprar" href="/carrito" >Comprar</a>
                    </div>
                    <% } %>
                </section>
            </main>
            <%- include('partials/footer.ejs') %>
    </body>